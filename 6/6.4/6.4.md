  > [6. Diseño Físico](../6.md) › [6.4. Módulo / Integrante 4](6.4.md)

# 6.4. Módulo de Gestión de Operaciones Terrestres

## 𝄜 Tabla: OperacionTerrestre

### **1. Justificación de la selección**

La tabla **OperacionTerrestre** fue seleccionada para el análisis físico por las siguientes razones:

  * **Volumen de datos:** Se espera un crecimiento constante de operaciones terrestres por expansión de rutas y
    demanda de transporte.
  * **Criticidad:** Es el núcleo del módulo terrestre, vinculando vehículos, rutas, conductores y costos.
  * **Relevancia:** Relaciona múltiples entidades operativas y permite trazabilidad completa de cada viaje.

Por ello, se considera una **tabla de alta criticidad y movimiento** dentro del módulo terrestre.

### **2. Estimación de cantidad de registros**

#### **Supuestos:**

  * Año base: **1,200 operaciones terrestres registradas.**
  * Crecimiento anual:
      * Año 2 → **+12%**
      * Año 3 → **+15%**

#### **Proyección:**

| Año | Nuevas Operaciones | Total Operaciones | Crecimiento |
|:---|:---|:---|:---|
| 1 | 1,200 | 1,200 | --- |
| 2 | 144 | 1,344 | 12% |
| 3 | 201 | 1,545 | 15% |

-----

### **3. Script SQL de creación de tabla**

```sql
 CREATE TABLE operaciones_terrestres.OperacionTerrestre (
     id_operacion_terrestre CHAR(36) PRIMARY KEY DEFAULT gen_random_uuid()::TEXT,
     id_operacion CHAR(36) NOT NULL UNIQUE,
     codigo VARCHAR(20) NOT NULL UNIQUE,
     id_vehiculo CHAR(36) NOT NULL,
     id_ruta_terrestre CHAR(36) NOT NULL,
     id_conductor CHAR(36) NOT NULL,
     costo_operacion_terrestre DECIMAL(10,2) NOT NULL,
     CONSTRAINT fk_op_terrestre_operacion FOREIGN KEY (id_operacion)
         REFERENCES operaciones_terrestres.Operacion(id_operacion)
         ON DELETE RESTRICT
         ON UPDATE CASCADE,
     CONSTRAINT fk_op_terrestre_vehiculo FOREIGN KEY (id_vehiculo)
         REFERENCES operaciones_terrestres.Vehiculo(id_vehiculo)
         ON DELETE RESTRICT
         ON UPDATE CASCADE,
     CONSTRAINT fk_op_terrestre_ruta FOREIGN KEY (id_ruta_terrestre)
         REFERENCES operaciones_terrestres.RutaTerrestre(id_ruta_terrestre)
         ON DELETE RESTRICT
         ON UPDATE CASCADE,
     CONSTRAINT fk_op_terrestre_conductor FOREIGN KEY (id_conductor)
         REFERENCES operaciones_terrestres.Conductor(id_conductor)
         ON DELETE RESTRICT
         ON UPDATE CASCADE,
     CONSTRAINT chk_costo CHECK (costo_operacion_terrestre >= 0)
 );
```

### **4. Estructura del registro**

| Atributo | Formato | Tamaño |
|:---|:---|:---|
| id\_operacion\_terrestre | CHAR(36) | 36 bytes |
| id\_operacion | CHAR(36) | 36 bytes |
| codigo | VARCHAR(20) | 20 bytes |
| id\_vehiculo | CHAR(36) | 36 bytes |
| id\_ruta\_terrestre | CHAR(36) | 36 bytes |
| id\_conductor | CHAR(36) | 36 bytes |
| costo\_operacion\_terrestre | DECIMAL(10,2) | 8 bytes |
| **Total estimado por registro:** | | **208 bytes** |

-----

### **5. Resumen de parámetros físicos**

| Parámetro | Valor |
|:---|:---|
| Tamaño de bloque | 8 KB |
| Estructura del registro lógico | 208 bytes |
| Overhead de fila | 23 bytes |
| Filas por bloque | 28 |
| Espacio libre reservado | `ALTER TABLE OperacionTerrestre SET (FILLFACTOR = 80);` |
| Tamaño total estimado (año 3) | ≈ 456 KB |

### **6. Cálculos**

  * **Espacio libre por bloque:** (8,192 − 24) × 0.8 = 6,534.4 bytes
  * **Cantidad de filas por bloque:** 6,534.4 ÷ (208 + 23) = 6,534.4 ÷ 231 ≈ 28.29 ≈ 28 filas por bloque
  * **Cantidad de bloques necesarios:** 1,545 ÷ 28 ≈ 55.18 ≈ 56 bloques
  * **Tamaño total estimado:** 56 × 8,192 = 458,752 bytes ≈ 456 KB

-----

## 𝄜 Tabla: Vehiculo

### **1. Justificación de la selección**

La tabla **Vehiculo** fue seleccionada por las siguientes razones:

  * **Volumen de datos:** Se espera un crecimiento progresivo de la flota terrestre por expansión de rutas y demanda de
    transporte.
  * **Criticidad:** Es clave para la planificación de operaciones, asignación de recursos y control logístico.
  * **Relevancia:** Se vincula directamente con operaciones terrestres, mantenimiento y seguimiento de activos.

Por ello, **Vehiculo** es una tabla fundamental en el sistema de transporte terrestre.

### **2. Estimación de cantidad de registros**

#### **Supuestos:**

  * Año base: **300 vehículos registrados.**
  * Crecimiento anual:
      * Año 2 → **+10%**
      * Año 3 → **+12%**

#### **Proyección:**

| Año | Nuevos Vehículos | Total Vehículos | Crecimiento |
|:---|:---|:---|:---|
| 1 | 300 | 300 | --- |
| 2 | 30 | 330 | 10% |
| 3 | 40 | 370 | 12% |

-----

### **3. Script SQL de creación de tabla**

```sql
 CREATE TABLE operaciones_terrestres.Vehiculo (
     id_vehiculo CHAR(36) PRIMARY KEY DEFAULT gen_random_uuid()::TEXT,
     id_activo CHAR(36) NOT NULL UNIQUE,
     placa VARCHAR(20) NOT NULL UNIQUE,
     capacidad_ton DECIMAL(10,2) NOT NULL,
     id_tipo_vehiculo CHAR(36) NOT NULL,
     id_estado_vehiculo CHAR(36) NOT NULL,
     CONSTRAINT fk_vehiculo_activo FOREIGN KEY (id_activo)
         REFERENCES operaciones_terrestres.Activo(id_activo)
         ON DELETE RESTRICT
         ON UPDATE CASCADE,
     CONSTRAINT fk_vehiculo_tipo FOREIGN KEY (id_tipo_vehiculo)
         REFERENCES operaciones_terrestres.TipoVehiculo(id_tipo_vehiculo)
         ON DELETE RESTRICT
         ON UPDATE CASCADE,
     CONSTRAINT fk_vehiculo_estado FOREIGN KEY (id_estado_vehiculo)
         REFERENCES operaciones_terrestres.EstadoVehiculo(id_estado_vehiculo)
         ON DELETE RESTRICT
         ON UPDATE CASCADE,
     CONSTRAINT chk_capacidad CHECK (capacidad_ton > 0)
 );
```

### **4. Estructura del registro**

| Atributo | Formato | Tamaño |
|:---|:---|:---|
| id\_vehiculo | CHAR(36) | 36 bytes |
| id\_activo | CHAR(36) | 36 bytes |
| placa | VARCHAR(20) | 20 bytes |
| capacidad\_ton | DECIMAL(10,2) | 8 bytes |
| id\_tipo\_vehiculo | CHAR(36) | 36 bytes |
| id\_estado\_vehiculo | CHAR(36) | 36 bytes |
| **Total estimado por registro:** | | **172 bytes** |

-----

### **5. Resumen de parámetros físicos**

| Parámetro | Valor |
|:---|:---|
| Tamaño de bloque | 8 KB |
| Estructura del registro lógico | 172 bytes |
| Overhead de fila | 23 bytes |
| Filas por bloque | 33 |
| Espacio libre reservado | `ALTER TABLE Vehiculo SET (FILLFACTOR = 80);` |
| Tamaño total estimado (año 3) | ≈ 96 KB |

### **6. Cálculos**

  * **Espacio libre por bloque:** (8,192 − 24) × 0.8 = 6,534.4 bytes
  * **Cantidad de filas por bloque:** 6,534.4 ÷ (172 + 23) = 6,534.4 ÷ 195 ≈ 33.51 ≈ 33 filas por bloque
  * **Cantidad de bloques necesarios:** 370 ÷ 33 ≈ 11.21 ≈ 12 bloques
  * **Tamaño total estimado:** 12 × 8,192 = 98,304 bytes ≈ 96 KB

[⬅️ Anterior](../6.3/6.3.md) | [🏠 Home](../../README.md) | [Siguiente ➡️](../6.4/6.4.1.md/6.4.1.md)